name: chrome_cd

on:
  workflow_dispatch:

jobs:
  zip:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/pnpm_install
      - run: pnpm -F @anlg/chrome zip
      - uses: actions/upload-artifact@v4
        with:
          name: chrome-extension-zip
          path: apps/chrome/.output/*.zip
          retention-days: 30
