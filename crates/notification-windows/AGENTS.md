# notification-windows

Custom always-on-top overlay notifications for Windows, matching the macOS collapsed/expanded notification surface.

The overlay is a layered Win32 popup window (not a system toast) so click, dismiss, timeout, options, footer, and expand behavior can map to the shared notification callbacks.
